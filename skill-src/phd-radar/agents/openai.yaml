interface:
  display_name: "PhD Radar"
  short_description: "Search, QS-screen, audit labs, and rank funded PhD opportunities"
  default_prompt: "Use $phd-radar. On first use, save my setup, search immediately, then generate, mount, and open the results dashboard automatically; otherwise reuse my profile and open the refreshed dashboard."
